\section{Results}

\subsection{Tissue-Specific Diffusivity Spectra}

The signal-to-noise ratio of the trace decays, computed as $S_0/\sigma$ from the per-ROI NUTS posterior, had a median of 303 across the 149~ROIs (interquartile range 176--478; full range 25--1548). (@patrick: make sure that these numbers are still accurate!!)
The wide spread reflects dependence on tissue composition (more free water, lower SNR (@patrick: chekc with stephan is this a fact that we self observed or something obvious that comes from literature? if the latter should we cite somethign or is this overkill?) and distance from the endorectal coil.
Figure~\ref{fig:spectra} shows MAP and NUTS spectral fractions (water fractions at each diffusivity bin, averaged across ROIs of each tissue type) (@patricK: paranthesis seems redundant again? please make sure to connec to other parts of manuscript) for tumor and normal tissue in both anatomical zones.
Tumor ROIs exhibited an elevated restricted-diffusion water fraction ($D = 0.25\;\umsmm$: 18.5\% MAP / 16.6\% NUTS in PZ tumor vs.\ 7.2\% / 5.7\% in PZ normal) and a reduced free-water fraction ($D = 3.0\;\umsmm$: 18.9\% MAP / 21.0\% NUTS in PZ tumor vs.\ 40.3\% / 47.5\% in PZ normal), consistent with increased cellular density restricting water mobility.
These patterns were qualitatively similar in the transition zone, though TZ tumor spectra showed a higher restricted fraction overall ($D = 0.25\;\umsmm$: 26.4\% MAP / 25.0\% NUTS) (@patrick: should make quantitiave comaprison to normal TZ here as well to make it ocnsistent with the PZ comparison above?). At the tuned regularization, MAP and NUTS agree closely on the two well-identified outer compartments. (@patrick: i know that a central part of the manuscript is the 2 bin collapse but should we desribe some of the intermeidate bins as well in more deatil here, as link to fig5 and gleason grade and aggressiveness discussion? make sure to not make it redudnant though)

\subsection{Per-Component Identifiability}
(@patrick: remember that Stepan said I didn't need as many subsections but should just make it separate paragraphs...this might also help reduce word count and be more concise and less redundant)
For each ROI, NUTS yields a full marginal posterior for each spectral fraction $R_j$; we summarize per-component identifiability by the posterior coefficient of variation (CV $=$ posterior std / posterior mean) (@patrick: redundant, netnioned before how we measure it, keep it brief and avoid regurgitating already steated facts), averaged across the patient cohort (the per-bin CV is shown by the bar shading in Figure~\ref{fig:sensitivity} and for every individual ROI in the supplementary atlas).
Only restricted diffusion ($D = 0.25\;\umsmm$, mean CV = 0.20) and free water ($D = 3.0\;\umsmm$, mean CV = 0.32) had cohort-averaged CV below 0.40.
The IVIM-perfusion proxy ($D = 20.0\;\umsmm$) was moderately constrained (CV = 0.36).
Intermediate components ($D = 0.50$--$1.00\;\umsmm$) had CV $> 0.80$, indicating posterior uncertainty comparable to the estimate itself.
This pattern is grounded in the Fisher information of the acquisition (Figure~\ref{fig:fisher}): the intermediate components are mutually correlated above $0.99$ and are therefore intrinsically ill-conditioned.
Simulation confirms that this is a data limit rather than an inference artifact: on simulated ground truths the sampler recovers smooth spectra accurately at the cohort SNR but cannot resolve a $\delta$-spike placed in the intermediate bins (@patricK: "cannot resolve" maybe a bit strong here, again try to be more nuanced and less black and white, but stay concise), while jointly recovering the noise level $\sigma$ to within $\sim$10\% except on that unrepresentable $\delta$ (Figure~\ref{fig:validation}). (@patrick: I wonder if we should take a slightly more naunced view here and look in more details at the intermediate bins as there are differnces in CV here as well right .. instead of classifying into bad and good (the sotry we keep getting back to and the 2 bin collapse) we coudl argue that 2.0 seems reasonabley well constrained also in fig4, and seems to play a role in grading as well right? also there might be slight differnce between TZ and PZ, e.g. for 1.00 or 0.5, again from fig4...just in general I want to leverage more the fact that we have the full spectrum and although it collapses to 2 for the detection task that does not mean that we have to only talk about htis thorughout the entire paper)

\subsection{Classification Performance}

Table~\ref{tab:auc} and Figure~\ref{fig:roc} summarize tumor-detection performance.
ADC was the strongest detector in both zones (raw rank PZ $0.951$ [$0.89$, $1.00$]; TZ $0.979$ [$0.94$, $1.00$]), but the spectral classifiers were statistically indistinguishable from it, with bootstrap confidence intervals overlapping ADC's throughout: the eight-fraction logistic regression reached PZ $0.926$ [$0.85$, $0.98$] (NUTS) and $0.917$ [$0.84$, $0.98$] (MAP), and TZ $0.923$ [$0.81$, $1.00$] (NUTS) and $0.952$ [$0.88$, $1.00$] (MAP).
Restricting the classifier to the two outer fractions $\{D = 0.25,\,3.0\;\umsmm\}$ did not reduce performance and, for some estimators, slightly improved it (@patrick: I am still slgihtly afarid that reviewers will ask about how one can be better than the LR, as it is optmized right, if intermadiate bins of 8 bins are harmful then they should be 0? also the raw vs LR is a bit confusing to me and should be made clearer, as we disussed in the classifciaotn waterproofing) (NUTS PZ $0.933$ [$0.86$, $0.98$], TZ $0.937$ [$0.85$, $0.99$]; MAP TZ $0.965$ [$0.90$, $1.00$]): the remaining six fractions carried no tumor-detection signal beyond the two outer compartments, and the collapse held for both MAP and NUTS.
Consistent with this, among the individual fractions only the two outer bins ($D = 0.25$ and $3.0\;\umsmm$) were strong single detectors, whereas the intermediate fractions were weak (@patrick: again i think this is slightly too pessimistic, if we look at raw performance of individual bins we actually see pretty high AUCs for some intermeidate bins, only d=20 for PZ and TZ, d=1.5 for TZ and d=2.0 for PZ seem close to chance behaviour, the rest are rather clsoe to the other curves as seen in fig 2...i would maybe want to slightly rething how we pitch this paper, the 2 bin collapse should not only be the center, there's a bit more that we can get out of the entire spectrum even though the middle bins are not as claer in terms identiabilty and detection perforamnce ... this coudl go a bit into the direciton of grading vs detection axes and some of the literature that we had looked at ... did we end up including it in the current paper? we should make sure during the literature pass) (Figure~\ref{fig:roc}).
We do not report grade-classification AUCs: with only $n = 29$ graded tumors they are too imprecise to be informative (95\% intervals spanning $\sim$0.5--0.95); the grading analysis is instead presented as grade-dependent spectral shifts (Figure~\ref{fig:spectrum_ggg}) and rank correlations below (@patrick: why is this better, maybe short justification for why we went for this kind of analysis instead).

These results were robust to regularization: at $C = 0.1$ and $C = 10.0$, AUC values varied by less than 0.03 across all tasks. (@patrick: also redundant if I am not mistaken?)

\subsection{ADC and the Spectral Discriminant}
(@patrick: also think about reducing subsections thorughout paper...)
Across ROIs, ADC values were near-perfectly anti-correlated with the spectral discriminant score in both zones and for both spectral estimators (Figure~\ref{fig:adc_discriminant}).
Using NUTS posterior-mean features at $C = 1.0$: PZ $r = -0.977$ (95\% bootstrap CI $[-0.985,\,-0.964]$, $n = 81$); TZ $r = -0.979$ (95\% CI $[-0.988,\,-0.971]$, $n = 68$).
Using constrained-MAP features at tuned regularization ($\lambda = 10^{-3}$): PZ $r = -0.969$ (95\% CI $[-0.981,\,-0.949]$); TZ $r = -0.959$ (95\% CI $[-0.976,\,-0.930]$).
We report bootstrap confidence intervals rather than $p$-values: with $|r| > 0.95$ and $n \geq 68$ the standard formula yields values below $10^{-39}$ that merely reflect sample size, whereas the intervals communicate the relevant quantity---how close the population correlation is to $-1$. (@patrick: is this extensive justification for bootstrap confidence isntad of p-value necessary? seems to be a standard choice and why metnion p value at all? instead we could spend a bit more time on some interesting obersvations in fig 3? or maybe that should be done discussion?)
This correlation was robust across regularization strengths ($C = 0.1$ to $50$; $|r| > 0.95$) and held equivalently for the MAP and NUTS feature representations (Figure~\ref{fig:adc_discriminant}).

\subsection{ADC Sensitivity Analysis}

The mechanistic basis for ADC's near-optimality is visible in the per-bin structure of the discriminant (Figure~\ref{fig:sensitivity}).
The ADC sensitivity vector $\partial\text{ADC}/\partial R_j$ at the average tumor operating point is strongly negative for restricted diffusion ($-1.59$, so a larger restricted fraction lowers ADC) and positive for free water ($+0.85$); since the logistic-regression coefficients point the opposite way, Figure~\ref{fig:sensitivity} plots the sign-flipped $-\partial\text{ADC}/\partial R_j$ alongside them, so aligned bars indicate matching direction.

Using NUTS features, the whole-profile correlation between the two vectors is $r = -0.79$ (PZ; bootstrap 95\% CI $[-0.89,\,-0.50]$) and $r = -0.88$ (TZ; $[-0.95,\,-0.70]$), and it is carried almost entirely by the two outer bins: only $D = 0.25$ and $D = 3.0\;\umsmm$ have coefficients whose bootstrap intervals exclude zero, whereas the intermediate coefficients are individually unstable on the ill-posed bins ($D = 0.5$--$1.0\;\umsmm$, CV $> 0.80$).
The much stronger alignment obtained under heavy regularization ($r \approx -0.98$ at $\lambda = 0.1$) is a property of that regularizer rather than an independent finding: smearing mass into the intermediate bins smooths the coefficient profile toward the monotonic shape of the sensitivity vector, and at the tuned MAP regularization and under NUTS the alignment is real but only moderate ($|r| \approx 0.8$). (@patrick: similar to the subsection before I am not sure if the last sentence is necesary / the right scope? regularization smearing while maybe interesting for one supplementary figure, as commented above is not really part of an interesting finding in this paper and is more a historic hurdle that we uncovered but that I am not sure if we should report in teh final manuscript. Rather again we should take a step back again and think how we can interpret or desribe fig4 in more detail and talk about the interesting thigns one see, of course this has to fit with figure desriptons...it might makes sense to write a short list of the main statments that I want to make and bring the readers attention to ,similar to the table we created to justify in one sentence every figure we have, should also be aupdated probably...finally Stephan metnioned that corelation of 0.8 is pretty good and we should prob not presnet it in this negative way)
\subsection{Spectral Decomposition by Gleason Grade Group}

Figure~\ref{fig:spectrum_ggg} shows the cohort's average NUTS posterior-mean spectrum by Gleason Grade Group, with normal tissue ($n = 109$) as the shared baseline and tumor resolved into GGG\,$=\,1$ ($n = 8$), GGG\,$=\,2$ ($n = 12$), and GGG\,$\geq 3$ ($n = 9$).
The grade ladder dissociates a detection axis from a grading axis.
The free-diffusion bin at $D = 3.0\;\umsmm$ (intact gland lumen) is suppressed as soon as tumor is present ($0.39$ normal $\to 0.21$ at GGG\,$=\,1$) and then changes little ($0.16$ at GGG\,$\geq 3$): it saturates at tumor onset, separating tumor from normal but not grade from grade.
By contrast the restricted-cellular bin ($D = 0.25\;\umsmm$) rises with grade ($0.07$ normal to $0.31$ at GGG\,$\geq 3$) and the glandular-epithelial bin ($D = 2.0\;\umsmm$) is preserved in low-grade tumor then depleted ($0.26$ normal, $0.28$ at GGG\,$=\,1$, $0.17$ at GGG\,$\geq 3$): these bins, where ADC is least sensitive (Figure~\ref{fig:sensitivity}), track aggressiveness.
Grade-specific sample sizes are small (down to $n = 8$--$9$) and the corresponding confidence bands wide, and 11 of the 40 tumor ROIs (7 graded GGG\,$=\,0$ and 4 ungraded) (@patrick: why are 8 discarded, should they not be part of normal split then?) are not included in this stratification.
Method robustness: re-running the same group averaging on tuned-MAP ($\lambda = 10^{-3}$) posterior estimates rather than NUTS posterior means yielded indistinguishable group trajectories on all eight bins.
(@patrick: I think this is a rather important subsection that diversifies our papers contribution a bit more and gives some epxloartory hypothesis about the intermeidate bins...lets make sure tha the interpretaitons are accuraet and Stephan agrees or has nothing more to add)
% ======================================================================
\subsection{Uncertainty-aware Classification}

Because NUTS yields a full posterior over each ROI's spectrum, the trained tumor-versus-normal classifier (the same leave-one-out logistic regression used for detection) (@patrick: repeating ourselves again...also maybe it makes sense to actually make this part of previous paragraph on classification performance? seems more related to PZ and TZ classifiaction or not? maybe we should also put current fig 6 a bit ealier in the paper to talk about PZ/TZ classification in Fig2, Fig4 and Fig6? ... then ggg analysis is something different with fig5 and fig7 and fig8 are definetly part of the same topic but might even be more connected to fig 1, so that we start with how we dreive the spectra and why they make sense (fig1, fig7 and 8), then we go into the whole TZ/PZ LR classifiation showing first the actual results with fig2, fig9 (the heatmap applied pixelwise as epxlroatory) and fig 6 (the unceratinty property and missclassification and closer to boundary unceraitnty things...) then related but somewhat different I see a sort of post analysis of the spectral classifier we built in fig3 and fig4 (this is where we no longer show performance on our cohort but we show these two figures for the core point of "why adc works" with the correlation of ROIs and sensitivity analysis) and finally as last part of the paper we show the ggg add on with current fig 5 ... this is an important comment please help me re-discuss this as ti touches upon the general structuring of the manuscript!) can be applied to every posterior draw rather than to the posterior mean alone, turning each prediction into a distribution over the cancer probability $P(\mathrm{tumor})$ rather than a single label (Figure~\ref{fig:uncertainty}).
The resulting 90\% credible interval on $P(\mathrm{tumor})$ was on average $2.4\times$ (@patrick: importnat this needs re-checking as I think with our last discussion we realized that we mgiht ahve to account for the geometric effects of LR in general so wider unceratinty clsoer to the decision boundary independt of varying posteriro unceratinty of the fractions ... also as disussed I want to empohasize two spectral unceratinty phenoma here ideally, one is the differencd between missclassifeid and classified, hioping that we have more uncerainty for the unclassified and second one for some subrange that is further away form decisoin boundary and one that is closer to decision boundary, also hoping that the LR geometric effects subtracted the posterior unceraitny does give us some hints here and can again be interepreted as an additonal singal that tells us about hte confidence of tumor) wider for misclassified than for correctly classified ROIs (pooled across zones, mean width $0.39$ vs $0.16$; Welch $p = 0.003$ (@patrick: for metodlogical consistency why do we use p value here all of a sudden, arent' we suing bootstrap CI also in the figure, or are these two different things? also what does "Welch" mean?), so the width of the propagated interval flags the ROIs the classifier is most likely to get wrong.
The few exceptions---ROIs misclassified with high confidence and a narrow interval---were almost exclusively low-grade (GGG\,$=\,1$) or ungraded tumors whose spectra overlap those of normal tissue (Figure~\ref{fig:spectrum_ggg}) (@patrick: a slightly more rigourous small deep dive into what properties the missclassified ROIs might have could be a valuable addition here ... maybe we cna match with something in our metadata or look at their individual spectra ... there are a few exceptions that are escaping our logic openly, e.g. a tumor case taht is very confidently classified as benign ... a comparison with GGG if availalbe is a good technique here it seems ... and of course lets make sure we did not make any labeling errors).
We present this analysis as an exploratory illustration of a capability unique to full posterior sampling (make this more tangible ... we actually get a cancer likelihood and uncertiaty estimates for free, shown by the two comapriosns of missclassifeid vs classifeid and distance from decision boundary); the closed-form MAP point estimate cannot produce these intervals (@patrick: an intersting observation that should not be the last sentence here i think again a bit too negative...while it should be highlighted somehwere it is not the main takeaway of this paragraph).

% ======================================================================
\subsection{Pixel-wise Spectral Decomposition}

Pixel-wise spectral estimation was performed on the manually segmented prostate of the supplementary patient (@patrick: again check with before ... aptient part of same cohrot i believe ... accoridng to resutrturing should maybe be merged with a previous paragraph) (MAP and NUTS; all NUTS voxels converged, $\hat{R} < 1.05$).

The restricted-diffusion fraction ($D = 0.25\;\umsmm$) showed the strongest spatial contrast among components (Figure~\ref{fig:pixelwise}), with the free-water fraction ($D = 3.0\;\umsmm$) approximately inverse, consistent with larger extracellular spaces in normal tissue.

(@patrick: entire paragraph very redundant again ... lets focus only on thigns that are unique to fig 9 not all the rest we have already stated previously... the unceraitny maps are something here, also the diff between the 2bin vs 8bin uncerainty difference (on average higehr for 8bin), also the interesting fact that areas of cancer are also more certain which is a good sign and helps give confidence in tumore regions ... then some interpretaion of the raw individual features vs the discrmantns ... and some comaprison to the classical ADC score...)
The per-voxel spectral discriminant (ROI-level PZ coefficients applied to pixel spectra) was near-perfectly anti-correlated with ADC ($r = -0.97$), and propagating the NUTS posterior through it yielded per-voxel uncertainty maps flagging where classification is least reliable (Figure~\ref{fig:pixelwise}).
Direction independence was verified at the whole-ROI level on a separate three-direction acquisition (Figure~\ref{fig:directions}): across all 13 regions the per-direction coefficient of variation of the estimated spectrum was lowest for the diagnostically dominant restricted-diffusion bin ($D = 0.25\;\umsmm$, mean $9\%$) and was large only on the near-empty bins ($D = 20\;\umsmm$), where it reflects a small-denominator effect rather than genuine anisotropy, confirming that trace averaging is appropriate for spectral decomposition.
